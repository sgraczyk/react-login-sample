import styled from 'styled-components';
import StyledVariables from '../../constants/styled-variables';

const ValidationError = styled.span`
  color: ${StyledVariables.colors.error};
  margin-bottom: 0.3em;
`;

export default ValidationError;
