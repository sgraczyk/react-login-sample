import styled from 'styled-components';
import StyledVariables from '../../constants/styled-variables';

const H2 = styled.h2`
  font-weight: 700;
  color: ${StyledVariables.colors.dark};
  text-align: center;
  line-height: 1.5em;
  margin-bottom: 1.2em;
  margin-top: 0.2em;
`;

export default H2;
