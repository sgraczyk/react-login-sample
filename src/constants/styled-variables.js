const colors = {
  primary: '#4CAF50',
  secondary: '#4CAFAA',
  white: '#FFFFFF',
  dark: '#384047',
  error: '#FF0000',
};

const properties = {
  borderRadius: '8px',
};

const StyledVariables = {
  colors,
  properties,
};

export default StyledVariables;
